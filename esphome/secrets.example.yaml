# Copy to secrets.yaml locally. Replace EVERY example value before flashing.
wifi_ssid: "CHANGE_ME"
wifi_password: "CHANGE_ME_WIFI_PASSWORD"
# Example key is public and ONLY for build validation. Generate a fresh key:
# python3 -c 'import secrets, base64; print(base64.b64encode(secrets.token_bytes(32)).decode())'
weatherstation_api_key: "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA="
weatherstation_ota_password: "CHANGE_ME_OTA_PASSWORD"
weatherstation_ap_password: "CHANGE_ME_AP_PASSWORD"
